@model IEnumerable<BDEstagio.Dominio.Tarefas>

@{
    ViewBag.Title = "Lista de Tarefas";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Cadastrar", "Cadastrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TarefaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarefa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TarefaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarefa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { /* id=item.PrimaryKey */ })
           
        </td>
    </tr>
}

</table>
